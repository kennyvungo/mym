

## Getting started
1. Navigate to project's root directory.

2. Run Express Backend

    ```cd backend ```
    ```npm install ```
    ```npm run dev ```
3. Run React Frontend

    ```cd frontend```
    ```npm install```
    ```npm start ```

